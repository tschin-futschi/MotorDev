# Plan

Stage: group_hover_and_configfile
Status: active
Priority: medium
Source: Claude Code
Date: 2026-04-02 07:00:00
Depends-On: review_button_feedback_20260402_063000.md (pass)
Supersedes: none

## Goal

两项 UI 调整：1) GroupBox hover 背景变色；2) Config File 去掉 GroupBox 包裹，改为紧凑一行。

## Scope

- 修改 `makePanelGroup` 函数的 QSS，添加 `QGroupBox:hover` 规则
- 移除 Config File 的 GroupBox 包裹，改为一个带标签的水平行直接放在 lowerArea 中

## Non-Goals

- 不修改按钮、信号槽连接
- 不使用 eventFilter 方案
- 不修改 IC / Serial / PMIC 三个 GroupBox 的布局结构

## Files-In-Scope

- `src/tabs/configtab.cpp`（makePanelGroup + setupUi + createConfigFileGroup 改造）
- `src/tabs/configtab.h`（如需移除 createConfigFileGroup 声明）

## Files-Frozen

- `src/serialmanager.h` / `src/serialmanager.cpp`
- `src/frameparser.h` / `src/frameparser.cpp`
- `src/devicecontext.h` / `src/devicecontext.cpp`
- `src/mainwindow.h` / `src/mainwindow.cpp`
- `src/main.cpp`
- `src/ui/style_constants.h`
- `src/widgets/*.h` / `src/widgets/*.cpp`
- `CMakeLists.txt`

## Constraints

- GroupBox hover 纯 QSS 实现
- Config File 行仍保留现有 4 个控件（m_fileCombo, m_browseButton, m_writeButton, m_readButton），只改布局容器

---

## 详细设计

### 1. GroupBox hover

修改 `configtab.cpp` 中 `makePanelGroup` 函数（约第 83-94 行）。

**当前样式：**
```cpp
group->setStyleSheet(QStringLiteral(
    "QGroupBox { background:%1; border:%2px solid %3; padding-top:28px; color:%4; font-size:14px; font-weight:500; }"
    "QGroupBox::title { subcontrol-origin: padding; subcontrol-position: top center; padding:8px 0 4px 0; }")
```

**改为：**
```cpp
group->setStyleSheet(QStringLiteral(
    "QGroupBox { background:%1; border:%2px solid %3; padding-top:28px; color:%4; font-size:14px; font-weight:500; }"
    "QGroupBox:hover { background:#f5f5f2; }"
    "QGroupBox::title { subcontrol-origin: padding; subcontrol-position: top center; padding:8px 0 4px 0; }")
```

- 正常背景：`#ffffff`（PanelBackground）
- Hover 背景：`#f5f5f2`（微妙暖灰变化）

### 2. Config File 改为紧凑一行

#### 2.1 移除 createConfigFileGroup 方法

删除 `configtab.h` 中的 `createConfigFileGroup()` 声明。
删除 `configtab.cpp` 中的 `createConfigFileGroup()` 方法实现。

#### 2.2 新增 createConfigFileRow 方法

在 `configtab.h` 中新增：
```cpp
QWidget *createConfigFileRow();
```

在 `configtab.cpp` 中新增：
```cpp
QWidget *ConfigTab::createConfigFileRow() {
    auto *row = new QWidget(this);
    auto *layout = new QHBoxLayout(row);
    layout->setContentsMargins(0, 0, 0, 0);
    layout->setSpacing(Style::Size::FormSpacing);

    auto *label = makeFormLabel(tr("Config File"), row);
    layout->addWidget(label);

    m_fileCombo = makeCombo({}, row);
    m_fileCombo->setEditable(true);
    m_fileCombo->setInsertPolicy(QComboBox::NoInsert);
    m_fileCombo->setPlaceholderText(tr("Select config file"));
    layout->addWidget(m_fileCombo, 1);

    m_browseButton = new QPushButton(tr("Browse"), row);
    m_browseButton->setFixedHeight(Style::Size::SidebarButtonHeight);
    m_browseButton->setStyleSheet(QStringLiteral(
        "QPushButton { background:%1; border:%2px solid %3; border-radius:5px; color:%4; font-size:13px; padding:0 12px; }"
        "QPushButton:hover { background:#f0f0f0; }"
        "QPushButton:pressed { background:#e0e0e0; padding:1px 12px 0 12px; }")
            .arg(Style::Color::White.name())
            .arg(Style::Size::BorderThin)
            .arg(Style::Color::DefaultBorder.name())
            .arg(Style::Color::TopBarValueText.name()));
    layout->addWidget(m_browseButton);

    m_writeButton = makePrimaryButton(tr("Write"), row);
    m_readButton = makePrimaryButton(tr("Read"), row);
    layout->addWidget(m_writeButton);
    layout->addWidget(m_readButton);

    return row;
}
```

#### 2.3 修改 setupUi

将 lowerArea 部分从：
```cpp
auto *lowerArea = new QWidget(splitter);
auto *lowerLayout = new QVBoxLayout(lowerArea);
lowerLayout->setContentsMargins(0, 0, 0, 0);
lowerLayout->setSpacing(Style::Size::ContentSpacing);
lowerLayout->addWidget(createConfigFileGroup());
lowerLayout->addStretch();
```

改为：
```cpp
auto *lowerArea = new QWidget(splitter);
auto *lowerLayout = new QVBoxLayout(lowerArea);
lowerLayout->setContentsMargins(0, 0, 0, 0);
lowerLayout->setSpacing(Style::Size::ContentSpacing);
lowerLayout->addWidget(createConfigFileRow());
lowerLayout->addStretch();
```

同时，由于 Config File 不再是一个占空间的 GroupBox，splitter 的 stretch 比例可调整为更倾向上方：
```cpp
splitter->setStretchFactor(0, 4);
splitter->setStretchFactor(1, 1);
```

---

## Acceptance

1. **编译通过** — `mingw32-make -C build_make_qt -j4` 成功
2. **GroupBox hover** — 鼠标悬浮在 IC / Serial / PMIC GroupBox 上时，背景色变为 `#f5f5f2`
3. **Config File 紧凑** — Config File 不再有 GroupBox 包裹，显示为一行：`Config File` 标签 + ComboBox + Browse + Write + Read
4. **控件保持完整** — m_fileCombo / m_browseButton / m_writeButton / m_readButton 仍为成员变量，功能不变
5. **冻结文件未修改**

## Notes

- 如果 GroupBox hover 在子控件上失效（Qt QSS 已知问题），后续可升级为 eventFilter 方案
