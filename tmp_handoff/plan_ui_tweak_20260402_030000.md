# Plan

Stage: ui_tweak
Status: active
Priority: high
Source: Claude Code
Date: 2026-04-02 03:00:00
Depends-On: review_heartbeat_defer_20260402_015000.md (pass)
Supersedes: none

## Goal

三项 UI 调整：全局字体增大、GroupBox 标题移至内部顶部居中、IC 框功能增强（增加 Scan / Connect 按钮，Slave ID 改为 ComboBox）。

## Scope

- 全局字体大小提升（所有包含 `font-size` 的 stylesheet）
- GroupBox 标题样式调整（标题在边框**内部**顶部居中显示）
- IC Group 增加 I2C Scan 按钮和 Connect 按钮
- Slave ID 从 QLineEdit 改为 QComboBox（由 Scan 结果填充）

## Non-Goals

- 不修改业务逻辑（SerialManager / FrameParser / DeviceContext 不改）
- 不修改信号槽连接逻辑（仅 IC 框相关连接需适配新控件）
- 不修改协议

## Files-In-Scope

- `src/ui/style_constants.h` (可选：如需新增字体相关常量)
- `src/tabs/configtab.h` (修改：成员变量变更)
- `src/tabs/configtab.cpp` (修改：主要改动文件)
- `src/widgets/activitybar.cpp` (修改：字体大小)
- `src/widgets/topbar.cpp` (修改：字体大小)
- `src/widgets/sidebar.cpp` (修改：字体大小)
- `src/mainwindow.cpp` (修改：字体大小)
- `src/tabs/registerrwtab.cpp` (修改：字体大小)
- `src/tabs/fwflashtab.cpp` (修改：字体大小)
- `src/tabs/oscilloscoptab.cpp` (修改：字体大小)

## Files-Frozen

- `src/serialmanager.h` / `src/serialmanager.cpp`
- `src/frameparser.h` / `src/frameparser.cpp`
- `src/devicecontext.h` / `src/devicecontext.cpp`
- `src/main.cpp`
- `CMakeLists.txt`

## Constraints

- 字体大小只改 stylesheet 中的 `font-size` 值，不设置全局 QApplication::setFont
- GroupBox 标题用 QSS 实现，不引入额外 QLabel 或自定义绘制
- Slave ID 改为 QComboBox 后，DeviceContext 接口不变（仍接受 uint8_t）
- IC Scan / Connect 按钮暂时只做 UI 占位和日志输出，不实现实际 I2C 扫描通信（需 STM32 固件支持）

---

## 详细设计

### 1. 字体大小调整

所有 stylesheet 中的 `font-size` 按以下映射修改：

| 位置 | 文件 | 现在 | 调整后 |
|------|------|------|--------|
| ActivityBar 按钮 | `activitybar.cpp:22` | 9px | 11px |
| StatusBar 标签 | `mainwindow.cpp:73` | 10px | 12px |
| Sidebar header | `sidebar.cpp:40` | 10px | 12px |
| ConfigTab form label (`makeFormLabel`) | `configtab.cpp:32` | 11px | 13px |
| ConfigTab button (`makePrimaryButton`) | `configtab.cpp:70` | 11px | 13px |
| ConfigTab GroupBox (`makePanelGroup`) | `configtab.cpp:89` | 12px | 14px |
| ConfigTab Browse button | `configtab.cpp:495` | 11px | 13px |
| TopBar title | `topbar.cpp:49` | 13px | 15px |
| TopBar port label | `topbar.cpp:52` | 11px | 13px |
| TopBar port value | `topbar.cpp:54` | 11px | 13px |
| TopBar connection label | `topbar.cpp:60` | 11px | 13px |
| RegisterRwTab sidebar label | `registerrwtab.cpp:33` | 11px | 13px |
| RegisterRwTab placeholder | `registerrwtab.cpp:49` | 18px | 20px |
| FwFlashTab sidebar label | `fwflashtab.cpp:33` | 11px | 13px |
| FwFlashTab placeholder | `fwflashtab.cpp:49` | 18px | 20px |
| OscilloscopTab sidebar label | `oscilloscoptab.cpp:33` | 11px | 13px |
| OscilloscopTab placeholder | `oscilloscoptab.cpp:49` | 18px | 20px |

### 2. GroupBox 标题样式调整

修改 `configtab.cpp` 中的 `makePanelGroup` 函数。

**当前代码：**
```cpp
group->setStyleSheet(QStringLiteral(
    "QGroupBox { background:%1; border:%2px solid %3; margin-top:12px; color:%4; font-size:12px; font-weight:500; }"
    "QGroupBox::title { subcontrol-origin: margin; left:%5px; padding:0 4px; }")
```

**改为：**
```cpp
group->setStyleSheet(QStringLiteral(
    "QGroupBox { background:%1; border:%2px solid %3; padding-top:28px; color:%4; font-size:14px; font-weight:500; }"
    "QGroupBox::title { subcontrol-origin: padding; subcontrol-position: top center; padding:8px 0 4px 0; }")
```

关键变化：
- 移除 `margin-top:12px`，改为 `padding-top:28px` 给标题留出空间
- `subcontrol-origin` 从 `margin` 改为 `padding`（标题在边框内部）
- `subcontrol-position` 设为 `top center`（居中）
- 标题上方 padding 8px，下方 4px

同时，所有使用此 GroupBox 的布局中 `contentsMargins` 的 top 值需要增加，因为标题现在占据内部空间。将各 `createXxxGroup()` 中的 `Style::Size::GroupBoxTopMargin`（10px）调整为 **8px**（标题已经通过 padding-top 留了空间，内容区 margin 可适当减小）。

### 3. IC 框功能增强

#### 3.1 configtab.h 成员变量变更

```diff
- QLineEdit *m_slaveIdEdit = nullptr;
+ QComboBox *m_slaveIdCombo = nullptr;
+ QPushButton *m_icScanButton = nullptr;
+ QPushButton *m_icConnectButton = nullptr;
```

移除 `QLineEdit` 前置声明（如果不再需要），确保 `QComboBox` 前置声明存在。

#### 3.2 configtab.cpp — createIcGroup() 改造

**当前结构：**
- IC 选择下拉框
- Slave ID 输入框（QLineEdit）

**改为：**
- IC 选择下拉框
- Slave ID 下拉框（QComboBox，placeholder "Scan first"，初始为空）
- 按钮行：Scan + Connect

```cpp
QGroupBox *ConfigTab::createIcGroup() {
    auto *group = makePanelGroup(tr("IC"), this);
    auto *layout = new QVBoxLayout(group);
    layout->setContentsMargins(
        Style::Size::ContentPadding,
        8,
        Style::Size::ContentPadding,
        Style::Size::ContentPadding);
    layout->setSpacing(Style::Size::FormSpacing);
    layout->setAlignment(Qt::AlignTop);

    auto *formLayout = new QFormLayout;
    formLayout->setLabelAlignment(Qt::AlignLeft | Qt::AlignVCenter);
    formLayout->setFormAlignment(Qt::AlignLeft | Qt::AlignTop);
    formLayout->setHorizontalSpacing(Style::Size::ContentSpacing);
    formLayout->setVerticalSpacing(Style::Size::FormSpacing);

    m_icCombo = makeCombo({QStringLiteral("AW86006"), QStringLiteral("DW9786"), QStringLiteral("DW9788")}, group);
    formLayout->addRow(makeFormLabel(tr("Select IC"), group), m_icCombo);

    m_slaveIdCombo = makeCombo({}, group);
    m_slaveIdCombo->setPlaceholderText(QStringLiteral("Scan first"));
    formLayout->addRow(makeFormLabel(QStringLiteral("Slave ID"), group), m_slaveIdCombo);

    layout->addLayout(formLayout);

    auto *buttonRow = new QHBoxLayout;
    buttonRow->setSpacing(Style::Size::FormSpacing);
    m_icScanButton = makePrimaryButton(tr("Scan"), group);
    m_icConnectButton = makePrimaryButton(tr("Connect"), group);
    buttonRow->addWidget(m_icScanButton);
    buttonRow->addWidget(m_icConnectButton);
    buttonRow->addStretch();
    layout->addLayout(buttonRow);
    return group;
}
```

#### 3.3 configtab.cpp — connectSignals() 适配

**移除的连接：**
- `m_slaveIdEdit` 的 `editingFinished` 连接
- `m_deviceContext->slaveIdChanged` 对 `m_slaveIdEdit` 的同步

**新增/修改的连接：**

```cpp
// IC Scan 按钮 — 暂时只输出日志占位
connect(m_icScanButton, &QPushButton::clicked, this, [this]() {
    qDebug() << "I2C scan started (stub)";
    // 未来实现：通过 SerialManager 发送 I2C 扫描命令
    // 扫描结果填充 m_slaveIdCombo
});

// IC Connect 按钮 — 暂时只输出日志占位
connect(m_icConnectButton, &QPushButton::clicked, this, [this]() {
    const QString addr = m_slaveIdCombo->currentText().trimmed();
    if (addr.isEmpty()) {
        qWarning() << "IC connect failed: no slave address selected";
        return;
    }
    qDebug().noquote() << QStringLiteral("IC connect to %1 (stub)").arg(addr);
    // 未来实现：通过 SerialManager 发送连接命令
});

// Slave ID ComboBox → DeviceContext 同步
connect(m_slaveIdCombo, &QComboBox::currentTextChanged, this, [this](const QString &text) {
    if (text.isEmpty()) {
        return;
    }
    bool ok = false;
    const uint value = text.toUInt(&ok, 16);
    if (ok && value <= 0x7F) {
        qDebug().noquote() << QStringLiteral("Slave ID set to 0x%1")
                                  .arg(value, 2, 16, QLatin1Char('0'))
                                  .toUpper();
        m_deviceContext->setSlaveId(static_cast<uint8_t>(value));
    }
});

// DeviceContext → Slave ID ComboBox 反向同步
connect(m_deviceContext, &DeviceContext::slaveIdChanged, this, [this](uint8_t id) {
    const QSignalBlocker blocker(m_slaveIdCombo);
    if (id == 0x00) {
        m_slaveIdCombo->setCurrentIndex(-1);
        return;
    }
    const QString text = QStringLiteral("0x%1").arg(id, 2, 16, QLatin1Char('0')).toUpper();
    const int index = m_slaveIdCombo->findText(text);
    if (index >= 0) {
        m_slaveIdCombo->setCurrentIndex(index);
    }
});
```

**初始化同步也需更新：**

移除原来对 `m_slaveIdEdit` 的初始值设置，改为：
```cpp
// m_slaveIdCombo 初始为空（等待 Scan 填充），不主动设置
```

---

## Acceptance

1. **编译通过** — `mingw32-make -C build_make_qt -j4` 成功
2. **字体增大** — 所有 UI 文字明显比修改前更大，层级关系保持不变
3. **GroupBox 标题** — 标题在边框内部顶部居中显示，不再骑在边框线上
4. **IC 框** — 包含 Select IC 下拉框、Slave ID 下拉框（初始为空）、Scan 按钮、Connect 按钮
5. **Scan/Connect 按钮** — 点击后控制台有日志输出
6. **冻结文件未修改**

## Notes

- IC Scan 和 Connect 按钮目前为占位实现（stub），待 STM32 固件支持后再接入实际 I2C 通信
- Slave ID 下拉框未来由 Scan 结果自动填充，当前为空
- 如果 `padding-top:28px` 导致视觉效果不理想，可微调数值（24px~32px 范围内尝试）
